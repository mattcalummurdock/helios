# Copy to frontend/.env — frontend-only variables (do not copy the repo-root .env here)

# Live stack (local Docker or Render backend)
# NEXT_PUBLIC_API_URL=http://localhost:8080

# Static Vercel demo — bundled JSON under public/demo/ (run ml/scripts/export_demo_static.py)
NEXT_PUBLIC_DEMO_MODE=true

NEXT_PUBLIC_CESIUM_ION_TOKEN=
